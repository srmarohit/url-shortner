import os
# from dotenv import load_dotenv

# load_dotenv()  # Load environment variables from .env file
# Do not push code into the repo.
# OAuth2 setup
CLIENT_ID = "########"
CLIENT_SECRET = "######"
REDIRECT_URI = "http://localhost:8000/github/callback"
